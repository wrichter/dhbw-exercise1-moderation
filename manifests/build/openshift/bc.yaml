apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  labels:
    app: moderation
    app.kubernetes.io/component: moderation
    app.kubernetes.io/instance: moderation
    app.kubernetes.io/part-of: dhbw-exercise-1
  name: moderation
spec:
  source:
    contextDir: /
    git:
      uri: https://github.com/wrichter/dhbw-exercise1-moderation
      ref: work
    type: Git
  strategy:
    type: Docker
    dockerStrategy: {}
    # type: Source
    # sourceStrategy:
    #   from:
    #     kind: ImageStreamTag
    #     name: nodejs:12
    #     namespace: openshift
  output:
    to:
    #   kind: DockerImage
    #   name: replace/me/moderation:latest
    # pushSecret:
    #   name: external-registry-dockercfg
      kind: ImageStreamTag
      name: moderation:latest
  failedBuildsHistoryLimit: 5
  postCommit: {}
  resources: {}
  runPolicy: Serial
  successfulBuildsHistoryLimit: 5
  triggers:
  - type: Generic
    generic:
      secretReference:
        name: moderation-generic-webhook-secret
  - type: GitHub
    github:
      secretReference:
        name: moderation-github-webhook-secret  
  # - type: ImageChange
  #   imageChange:
  #     lastTriggeredImageID: image-registry.openshift-image-registry.svc:5000/openshift/nodejs@sha256:87be8d1bf03ef6cf2d050ff86ea1798a6a40f0bf238d1d8b7eadb5f45530de36
  - type: ConfigChange